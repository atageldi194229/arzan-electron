{
  "name": "arzan-electron",
  "version": "1.0.1",
  "description": "Hello World",
  "main": "main.js",
  "scripts": {
    "start": "electron main.js",
    "build": "electron-packager . app --platform win32 --arch x64 --out dist/ --overwrite",
    "setup": "electron-installer-windows --src dist/app-win32-x64/ --dest dist/installers/"
  },
  "keywords": [],
  "author": "Atasan",
  "license": "ISC",
  "devDependencies": {
    "electron": "^19.0.4",
    "electron-installer-windows": "^3.0.0"
  },
  "dependencies": {
    "auto-launch": "^5.0.5",
    "electron-squirrel-startup": "^1.0.0"
  }
}
